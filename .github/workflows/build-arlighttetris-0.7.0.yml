name: Build ArlightTetris 0.7.0

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Gradle 8.14
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.14'

      - name: Rebuild source
        run: |
          cat phase1/source.fix00a phase1/source.fix00b \
              phase1/source.part01 phase1/source.part02 phase1/source.part03 \
              phase1/source.part04 phase1/source.part05 phase1/source.part06 \
              phase1/source.part07 > joined.b64
          wc -c joined.b64
          sha256sum joined.b64
          test "$(wc -c < joined.b64)" -eq 98136
          test "$(sha256sum joined.b64 | cut -d' ' -f1)" = "a7a020648e66cd44f486597a96988849c02072ebb99f6b1b19d96cbe050873c4"
          base64 -d joined.b64 > source.zip
          unzip -t source.zip
          mkdir project
          unzip -q source.zip -d project
          if [ -d phase1/overrides ]; then cp -a phase1/overrides/. project/; fi
          test -f project/build.gradle
          grep -q "version = '0.7.0'" project/build.gradle

      - name: Compile
        working-directory: project
        run: gradle --no-daemon clean build

      - name: Prepare artifact
        run: |
          mkdir -p artifact
          JAR=$(find project/build/libs -maxdepth 1 -type f -name '*.jar' ! -name '*sources*' ! -name '*dev*' | head -n 1)
          test -n "$JAR"
          cp "$JAR" artifact/ArlightTetris-0.7.0-GAMEPLAY-PHASE1-COMPILED.jar
          ls -lh artifact

      - name: Upload compiled JAR
        uses: actions/upload-artifact@v4
        with:
          name: ArlightTetris-0.7.0-GAMEPLAY-PHASE1
          path: artifact/ArlightTetris-0.7.0-GAMEPLAY-PHASE1-COMPILED.jar
          if-no-files-found: error
          retention-days: 30
